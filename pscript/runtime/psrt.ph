# psrt.ph — o guarda-chuva do runtime.
#
# O programa gerado inclui UM header, e continua incluindo este: quem foi
# dividido (bateria 108) foram os módulos por baixo. A ordem é a das camadas —
# tipos, memória, valores, o que roda, a biblioteca, o sistema, o epílogo.
import "psrt_types.ph"
import "psrt_mem.ph"
import "psrt_val.ph"
import "psrt_rt.ph"
import "psrt_std.ph"
import "psrt_os.ph"
import "psrt_top.ph"
